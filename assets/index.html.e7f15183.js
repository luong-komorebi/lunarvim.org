import{e as n}from"./app.1d1627fc.js";import{_ as a}from"./plugin-vue_export-helper.21dcd24c.js";const s={},e=n(`<h1 id="plugins" tabindex="-1"><a class="header-anchor" href="#plugins" aria-hidden="true">#</a> Plugins</h1><h2 id="installing" tabindex="-1"><a class="header-anchor" href="#installing" aria-hidden="true">#</a> Installing</h2><p>Plugins are installed by adding snippets into the <code>lvim.plugins</code> table in your <code>config.lua</code> file. Save (<code>:w</code> or <code>&lt;leader&gt;w</code>), and Packer will autoinstall the new plugins.</p><div class="language-lua ext-lua line-numbers-mode"><pre class="language-lua"><code>lvim<span class="token punctuation">.</span>plugins <span class="token operator">=</span> <span class="token punctuation">{</span>
  <span class="token punctuation">{</span><span class="token string">&quot;lunarvim/colorschemes&quot;</span><span class="token punctuation">}</span><span class="token punctuation">,</span>
  <span class="token punctuation">{</span><span class="token string">&quot;folke/tokyonight.nvim&quot;</span><span class="token punctuation">}</span><span class="token punctuation">,</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Run <code>:PackerSync</code> to pull down updates for your existing plugins. The plugins are stored at <code>~/.local/share/lunarvim/site/pack/packer</code>. The README&#39;s (and <code>docs/</code> folder if it exists) typically contain excellent documentation, so it may be worthwhile to create an alias for this directory.</p><div class="custom-container tip"><p class="custom-container-title">Notice</p><p>see which plugins are installed with <code>:PackerStatus</code></p></div><h2 id="removing" tabindex="-1"><a class="header-anchor" href="#removing" aria-hidden="true">#</a> Removing</h2><p>Removing a plugin from the <code>lvim.plugin</code> table removes it from your configuration but not your system. Any plugins left in the <code>start</code> directory will still autostart. To remove them completely, run <code>:PackerClean</code></p><h2 id="configuration" tabindex="-1"><a class="header-anchor" href="#configuration" aria-hidden="true">#</a> Configuration</h2><div class="language-lua ext-lua line-numbers-mode"><pre class="language-lua"><code>lvim<span class="token punctuation">.</span>builtin<span class="token punctuation">.</span>lualine<span class="token punctuation">.</span>active <span class="token operator">=</span> <span class="token keyword">true</span>
lvim<span class="token punctuation">.</span>builtin<span class="token punctuation">.</span>dap<span class="token punctuation">.</span>active <span class="token operator">=</span> <span class="token keyword">true</span>
lvim<span class="token punctuation">.</span>builtin<span class="token punctuation">.</span>alpha<span class="token punctuation">.</span>active <span class="token operator">=</span> <span class="token keyword">true</span>
lvim<span class="token punctuation">.</span>builtin<span class="token punctuation">.</span>terminal<span class="token punctuation">.</span>active <span class="token operator">=</span> <span class="token keyword">true</span>
lvim<span class="token punctuation">.</span>builtin<span class="token punctuation">.</span>bufferline<span class="token punctuation">.</span>active <span class="token operator">=</span> <span class="token keyword">true</span> <span class="token comment">-- this is actually using romgrk/barbar.nvim</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Settings for core plugins are accessible through <code>lvim.builtin</code>. You can press <code>TAB</code> to get autocomplete suggestions to explore these settings.</p><p>If a plugin is lazy-loaded and you disable it, you need to let Packer know about it by running <code>:PackerSync</code>.</p><p><strong>Example settings</strong></p><div class="language-lua ext-lua line-numbers-mode"><pre class="language-lua"><code>lvim<span class="token punctuation">.</span>builtin<span class="token punctuation">.</span>cmp<span class="token punctuation">.</span>completion<span class="token punctuation">.</span>keyword_length <span class="token operator">=</span> <span class="token number">2</span>
lvim<span class="token punctuation">.</span>builtin<span class="token punctuation">.</span>telescope<span class="token punctuation">.</span>defaults<span class="token punctuation">.</span>layout_config<span class="token punctuation">.</span>width <span class="token operator">=</span> <span class="token number">0.95</span>
lvim<span class="token punctuation">.</span>builtin<span class="token punctuation">.</span>telescope<span class="token punctuation">.</span>defaults<span class="token punctuation">.</span>layout_config<span class="token punctuation">.</span>preview_cutoff <span class="token operator">=</span> <span class="token number">75</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div>`,14);function t(o,p){return e}var l=a(s,[["render",t]]);export{l as default};
